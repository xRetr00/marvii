[[permission]]
identifier = "allow-workspace-files"
description = "Allow opening, revealing, and previewing files resolved inside the active Marvi workspace"

[permission.commands]

allow = [
    "open_workspace_path",
    "reveal_workspace_path",
    "preview_workspace_text",
    "resolve_workspace_absolute_path",
]

deny = []
